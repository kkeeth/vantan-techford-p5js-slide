{
  "name": "slidev",
  "private": true,
  "scripts": {
    "build": "slidev build",
    "build-all": "rimraf dist && sh build.sh",
    "serve": "slidev --open",
    "dev": "slidev --open ${npm_config_doc}.md",
    "export": "slidev export",
    "predev": "cp slides/${npm_config_doc}.md ./"
  },
  "dependencies": {
    "@slidev/cli": "^0.36.5",
    "@slidev/theme-default": "*",
    "@slidev/theme-seriph": "*",
    "slidev-theme-penguin": "^1.4.1"
  },
  "devDependencies": {
    "@iconify-json/logos": "^1.1.5",
    "rimraf": "^3.0.2",
    "vite-svg-loader": "^3.6.0"
  }
}
